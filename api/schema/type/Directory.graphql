type Directory {
  id: ID!
  createdAt: String!
  updatedAt: String!
  name: String!
  parent: Directory
  isPublished: Boolean!
  children(sort: DirectorySort): [Directory]
  pages(sort: PageSort): [Page!]
}
