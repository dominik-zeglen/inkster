type Query {
  getDirectory(id: ID!): Directory
  getDirectories: [Directory]
  getRootDirectories: [Directory]

  page(id: ID!): Page
  pages: [Page]

  user(id: ID!): User
  users: [User]
  viewer: User
}
